# Proteins per Dataset
PREFIX schema: <https://schema.org/>

SELECT ?graph (COUNT(DISTINCT ?uniprot) AS ?Proteins)
WHERE {
    GRAPH ?graph {
        ?s a schema:Protein ;
            schema:sameAs ?uniprot .
    }
}
GROUP BY ?graph
